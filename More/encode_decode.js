const string = "Learning JavaScript";

const result = window.btoa(string);
console.log(result);

const result1 = window.atob
                        (string)
        console.log(result1);

        // this code will always run in inspect element 
        //  of chrome and other browsers
